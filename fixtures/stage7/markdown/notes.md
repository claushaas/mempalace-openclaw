# Markdown Recall Fixture

Decision: use MemPalace as the durable source of truth.

Problem: stale context can pollute prompt assembly when provenance is weak.

Milestone: recommended mode already proves observable pre-reply recall.
